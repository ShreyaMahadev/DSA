class Solution {
public:
    int fib(int n)
    {
        if(n==0 || n==1)
            return n;
        else
            return fib(n-1)+fib(n-2);

        
    }
};

Time Complexity : O(2^n) i.e exponential
Space Complexity : O(n)
