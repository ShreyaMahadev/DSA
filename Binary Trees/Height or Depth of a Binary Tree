Using Recursive Approach

class Solution {
public:
    int maxDepth(TreeNode* root) {
        if(root==NULL)  return 0;

        int lh=maxDepth(root->left);
        int rh=maxDepth(root->right);

        return 1+max(lh,rh);
        
    }
};

TC=O(N)
SC=O(H)=O(N) (worst case) or O(logN) (best case)
