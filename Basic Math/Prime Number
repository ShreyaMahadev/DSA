bool isPrime(int n) 
{
    if (n <= 1) return false;                   // 0, 1 and negatives are not prime

    for (int i = 2; i <= sqrt(n); i++)
    {
        if (n % i == 0) return false;
    }
    return true;
}
