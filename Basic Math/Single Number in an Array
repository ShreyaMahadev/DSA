// XOR Approach
a ^ a = 0 → same numbers cancel out
a ^ 0 = a → XOR with 0 gives the number itself

class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int ans=0;
        int n=nums.size();
        for(int i=0;i<n;i++)
        {
            ans^=nums[i];  // XOR current element with ans
        }
        return ans;  // Return the single number
        
    }
};

TC : O(N) → single traversal of array
SC : O(1)
